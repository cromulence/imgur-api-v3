dependencies {
    compileOnly libs.slf4j

    compile libs.gson
    compile libs.guava
    compile libs.httpclient

    compile project(':api')

    compile libs.datawrapperApi
    testCompile libs.datawrapperProperties

    testCompile libs.slf4jSimple
    testCompile libs.junit
}